import 'package:flutter/material.dart';

class SimulatedWatermark extends StatelessWidget {
  const SimulatedWatermark({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 16);
  }
}
